server:
  port: 9090

spring:
  application:
    name: authorization
  data:
    mongodb:
      uri: mongodb://localhost/qauthorization
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9000/oauth2/jwks
