spring:
  application:
    name: backend
  data:
    mongodb:
      uri: mongodb://localhost/qmessenger
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9000/oauth2/jwks